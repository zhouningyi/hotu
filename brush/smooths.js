'use strict';

// 粗细变化
define(['./easing', './../libs/event', './../utils/utils'], function (Easing, Event, Util) {
  var extend = Util.extend;

  function Smoothes(smoothes) {
    for 
  }

  Event.extend(Smoothes, {
  });

  return Smooth;
});